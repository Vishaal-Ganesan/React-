import { createTheme } from "@mui/material";

export const tyTheme = createTheme({
    typography:{
        fontFamily:'GrotesquePro Bold',
    }
})


export const tyThemeLight = createTheme({
    typography:{
        fontFamily:'GrotesquePro Thin',
    }
})